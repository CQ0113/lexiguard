{
  "name": "lexiguard-functions",
  "private": true,
  "version": "0.1.0",
  "description": "Cloud Functions scaffold for LexiGuard lawyer verification",
  "main": "src/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "lint": "echo \"No function lint configured\""
  },
  "dependencies": {
    "@google/genai": "^2.6.0",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^5.1.1"
  }
}
